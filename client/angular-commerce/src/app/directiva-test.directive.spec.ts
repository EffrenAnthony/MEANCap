import { DirectivaTestDirective } from './directiva-test.directive';

describe('DirectivaTestDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaTestDirective();
    expect(directive).toBeTruthy();
  });
});
